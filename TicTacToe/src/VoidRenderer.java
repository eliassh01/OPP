/**
 * Class which represent renderer that doesn't print anything.
 * It implements the interface Renderer.
 */
public class VoidRenderer implements Renderer{
    /**
     * function that doesn't do anything.
     * @param board object of type renderer.
     */
    public void renderBoard(Board board){}
}